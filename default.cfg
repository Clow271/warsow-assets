// default.cfg

// Movement
bind w "+forward"
bind s "+back"
bind a "+moveleft"
bind d "+moveright"
bind SPACE "+moveup"
bind LCTRL "+movedown"
bind LSHIFT "+zoom"
bind MOUSE1 "+attack"
bind MOUSE2 "+special"
bind LALT "+speed"

// Chat
bind ENTER "messagemode"
bind RSHIFT "messagemode2"
bind t "messagemode"
bind y "messagemode2"

// Weapons (generic)
bind MWHEELUP "weapprev"
bind MWHEELDOWN "weapnext"
bind 1 "use gb"
bind 2 "use mg"
bind 3 "use rg"
bind 4 "use gl"
bind 5 "use rl"
bind 6 "use pg"
bind 7 "use lg"
bind 8 "use eb"

// Weapons (quick access)
bind q "use rl"
bind e "use rg"
bind r "use lg"
bind f "use eb"
bind z "use pg"
bind x "use mg"
bind c "use gl"

// Actions
bind g "drop bomb"

// Functions
bind TAB "+scores"
bind F1 "vote yes"
bind F2 "vote no"
bind F3 "join"
bind F4 "toggleready"
bind F5 "chase"
bind F7 "score"
bind F8 "menu_callvotes"
bind F9 "callvote allready"
bind F12 "screenshot"
bind b "gametypemenu"

// Sound
set s_module 1

// Misc
set cg_viewBob 0
